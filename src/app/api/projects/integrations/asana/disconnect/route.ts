import { NextRequest } from "next/server"; import { integrationScope } from "@/lib/integration-auth"; import { getCredentialStore } from "@/modules/projects"; import { apiSuccess } from "@/lib/api"; export const runtime = "nodejs"; export async function POST(req: NextRequest) { const auth = await integrationScope(req, "integrations:manage"); if ("error" in auth) return auth.error; await getCredentialStore().deleteCredential(auth.scope, "ASANA"); return apiSuccess({ disconnected: true }); }
