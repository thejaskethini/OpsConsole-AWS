import { NextRequest } from "next/server"; import { integrationScope } from "@/lib/integration-auth"; import { RealAsanaProvider } from "@/modules/projects"; import { apiSuccess } from "@/lib/api"; export const runtime = "nodejs"; export async function GET(req: NextRequest) { const auth = await integrationScope(req, "integrations:read"); if ("error" in auth) return auth.error; return apiSuccess({ integration: await new RealAsanaProvider(auth.scope).getStatus() }); }
